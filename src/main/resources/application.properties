# Server Configuration
server.port=8080
spring.application.name=open-invite

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/openinvite
spring.datasource.username=mitchellmorgan
spring.datasource.password=Rb26dett
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=your-secret-key-change-this-in-production-make-it-at-least-256-bits-long-open-invite-app
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:3000

# Logging
logging.level.com.openinvite=DEBUG
logging.level.org.springframework.security=DEBUG